[build]
  command = "node scripts/netlify-typecheck-diagnostics.mjs"
  publish = "public"
  framework = "#custom"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  BOOKING_LOCAL_MODE = ""
  FIDELITY_FIXTURES = ""
