[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  # Never enable local booking fixtures in Netlify production builds.
  BOOKING_LOCAL_MODE = ""
  FIDELITY_FIXTURES = ""
