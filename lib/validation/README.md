# Validation schemas

This directory is reserved for input validation schemas. A library such as
Zod will be added in a later phase when user input (booking forms, contact
forms, checkout) is implemented.

Examples to be added:

- `booking-schema.ts`
- `contact-schema.ts`
- `workshop-schema.ts`
